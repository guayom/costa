<style>
  .navbar-default .navbar-nav > li > a {
    padding-top: 60px;
    padding-bottom: 60px;
  }
  .navbar-brand img {
    height: 140px;
  }

  .separator {
    margin-top: 80px;
  }
</style>
<script>
  $(window).on('scroll',function() {
    var scrollSize = Math.round($(window).scrollTop());
    console.log(scrollSize);
    if (scrollSize > 37) {
      $('.navbar-fixed-top').css({
        marginTop: 0
      });
    } else {
      $('.navbar-fixed-top').css({
        marginTop: 37 - scrollSize
      });
    }

    if (scrollSize > 60) {
      $('.navbar-default .navbar-nav > li > a').css({
        paddingTop: '30px',
        paddingBottom: '30px'
      });
//      $('.navbar-brand img').css({
//        height: '80px !important'
//      });
      $('.navbar-brand img').height(80);
      $('.separator').css({marginTop: '80px'});
    } else {
      $('.navbar-default .navbar-nav > li > a').css({
        paddingTop: (60 - scrollSize / 2) + 'px',
        paddingBottom: (60 - scrollSize / 2) + 'px'
      });
//      $('.navbar-brand img').css({
//        height: (140 - scrollSize / 2) + 'px !important'
//      });
      $('.navbar-brand img').height(140 - scrollSize / 2);
      $('.separator').css({marginTop: (140 - scrollSize / 2) + 'px'});
    }
  });
</script>

<div class="contact-header">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-8 header-phones">
        <strong class="hidden-xs">Teléfonos: </strong>
        <% CONTACT_INFO['telefonos'].each_with_index do |tel, index| %>
          <a href="tel:<%= tel['numero'] %>" style="white-space: nowrap;"><%= tel['numero'] %></a>
          <% unless index == CONTACT_INFO['telefonos'].size - 1 %>
            /
          <% end %>
        <% end %>
      </div>
      <div class="col-sm-4 text-right hidden-xs">
        <strong>Seguinos en: </strong>
        <% CONTACT_INFO['redes_sociales'].each do |link| %>
          <a href="<%= link['url'] %>" class="icon-<%= link['nombre'] %>" rel="tooltip" title="" data-placement="bottom" data-original-title="<%= link['nombre'].titleize %>" target="_blank">
            <i class="fa fa-<%= link['icon'] %>-official"></i>
          </a>
        <% end %>
      </div>
    </div>
  </div>
</div>

<!-- begin:navbar -->
<nav class="navbar navbar-default navbar-fixed-top" role="navigation" style="margin-top: 37px;">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-top">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <%#= image_tag("logo.jpg") %>
        <%= image_tag 'logos/logo_full.png' %>
      </a>
      <%#= image_tag("cccbr.png", class: "cccbr-logo") %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-top">
      <%= render_navigation %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</nav>
<!-- end:navbar -->

<div class="separator"></div>
